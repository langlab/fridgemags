$(function() { //jQuery load
	now.ready(function () {
		// window.frig = new Frig();
		// window.frig.mags = new Mags();
		// //window.mags.add([{word: 'hola'},{word: 'adios'}]);
		// window.frig.mags.view = new MagsView({collection: window.frig.mags});
		// window.frig.mags.view.render();
		
		// now.cmags = {
		// 	add: function(m) {
		// 		console.log(m,' added from server');
		// 		window.mags.add(m);
		// 	},
		// 	
		// 	update: function(m) {
		// 		console.log(m,' updated from server');
		// 	}
		// }
	});
});